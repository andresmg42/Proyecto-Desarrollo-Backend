apiVersion: apps/v1
kind: Deployment
metadata: 
  name: django-api-deployment-v2
spec:
  replicas: 2
  selector: 
    matchLabels:
      app: django-api
      version: v2
  template:
    metadata:
      labels:
        app: django-api
        version: v2
    spec:
      containers:
        - name: classmart-backend
          image: andresmg42/classmart-backend:v2
          ports:
            - containerPort: 8000
          env:
          - name: IS_PRODUCTION
            value: "True"

          envFrom:
            - secretRef:
                name: postgres-secret